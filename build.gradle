plugins {
  id 'java-library'
}

task zip(type: Zip) {
	from('images/') {
		include '*'
		exclude 'store/'
		into('images')
	}
	from('lib/') {
		into('lib')
	}
	from('src/') {
		into('src')
	}
	from('/') {
		include 'background.js'
		include 'manifest.json'
	}
}